var Github = require('github');

module.exports = function () {
  this.getRepos = function (userName) {

  };

  this.getCommits(userName, repoName) {

  };

  this.getCommit(userName, repoName, commit) {

  };

  this.getStatistics = function ()
  {
    return {
      stats : [
        {
          extension : "js",
          lines : 12567
        },
        {
          extension : "cs",
          lines : 125
        }
      ]
    };
  };
};
